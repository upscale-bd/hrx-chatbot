apiVersion: v1
kind: Service
metadata:
  name: {{ include "hrx-chatbot.fullname" . }}
  labels:
    {{- include "hrx-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hrx-chatbot.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
