apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmx-backend.fullname" . }}
  labels:
    {{- include "pmx-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pmx-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
