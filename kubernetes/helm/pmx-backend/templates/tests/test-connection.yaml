apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pmx-backend.fullname" . }}-test-connection"
  labels:
    {{- include "pmx-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pmx-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
